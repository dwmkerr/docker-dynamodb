version: 2
jobs:
  build:
jobs:
  test:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: make test
  deploy:
    machine:
      docker_layer_caching: true    # default - false
    environment:
      BUILD_NUM: $CIRCLE_BUILD_NUM
    steps:
      - run: make deploy

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - deploy:
          requires:
            - test
          filters:
            branches:
              only: master
